class Debtor{
  int id;
  String name;
  double debt;
  double paid;
  DateTime duration;
  String address;
  String reason;
  

  Debtor(this.id, this.name, this.debt, this.paid, this.duration, this.address, this.reason);
}